extends layout

include mixins/handleProject 

block content
    .contenido-principal
        h1= pageName

        //- pre=vardump(project)

        if errors 
            each error in errors 
                .alerta.error= error.text 

        +handleProject(project) 